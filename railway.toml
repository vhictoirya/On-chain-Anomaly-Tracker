[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 75"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[variables]
PYTHON_VERSION = "3.13.0"